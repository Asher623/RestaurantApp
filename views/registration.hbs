<div class="divs">

<div class="modal" id = "logInModal">
  <div class="content">
    <form class="" action="register" method="POST">
      <p id = "CustomLog">Create Account</p>
      <div class="errors">
        {{# if success}}
          <h1>SUCCESS</h1>
        {{else}}
          {{# if errors}}
            <ul>
              {{regError}}
              {{# each errors}}
                <li>{{this.msg}}</li>
              {{/each}}
            </ul>

          {{/if}}
        {{/if}}
      </div>

      <label for="firstname">First Name</label>

      <input type="text" name="firstname" value="{{data.firstname}}">

      <label for="lastname">Last Name</label>

      <input type="text" name="lastname" value="{{data.lastname}}">

      <label for="email">Email</label>

      <input type="email" name="email" id = "email" value="{{data.email}}">

      <label for="password">Password</label>

      <input type="password" name="password" value="">

      <input id="build_a" class = "logButton" type="submit" name="button" value="SIGN UP">

    </form>
  </div>
</div>
<div class="logOnBenefits"  id = "regBenefits">
  <h1>Don't have an account?</h1>
  <p>Here are some of the benefits you’ll enjoy:</p>
  <h4>Fast checkout.</h4>
  <p>Your payment info is saved and ready.</p>
  <h4>Easy tracking.</h4>
  <p>Keep an eye on your order.</p>
  <h4>Quick recap.</h4>
  <p>Your order history is a click away.</p>

</div>
</div>
